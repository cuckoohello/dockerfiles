version: '2'
services:
  web:
    image: cuckoohello/rslsync
    restart: always
    volumes:
      - ./data:/data
    ports:
      - 55555:55555
      - 55555:55555/udp
      - 8080:8080
    container_name: rslsync
networks:
  default:
    external:
      name: service
